<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws the UI; this base theme just keeps the system happy
         before the Compose content takes over. -->
    <style name="Theme.PersonalAgent" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
